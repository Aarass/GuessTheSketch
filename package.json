{
  "name": "guessthesketch",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "client": "bun run --filter \"./packages/client\" start",
    "server": "bun run --filter \"./packages/server\" start",
    "all": "bun run --filter \"./packages/client\" --filter \"./packages/server\" start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react-router": "^7.2.0"
  }
}
